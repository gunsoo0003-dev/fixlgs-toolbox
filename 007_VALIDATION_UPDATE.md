# TOOLBOX 007 자동 검수기 보완

## 목적
사용자 수동 검수 없이 한 번의 명령으로 007의 기능, UI 흐름, 다국어, SEO, 파일 제한, 전체 처리, ZIP, 초기화 및 모바일 배치를 최대한 검증한다.

## 명령
```powershell
npm run test:toolbox:007
```

빌드까지 포함한 최종 검수:
```powershell
npm run test:toolbox:007-final
```

## 안전 운영 한도
- 파일 수: 10개
- 개별 파일: 15MB
- 전체 파일: 50MB
- 개별 이미지: 32MP
- 전체 이미지: 90MP
- 최대 한 변: 16,384px

위 값은 코드에서 `WEB_IMAGE_OPTIMIZER_LIMITS` 단일 상수로 관리하며 검수기가 동일 값을 읽어 경계값을 검증한다.

## 한 번에 확인하는 항목
- 한국어·영어·일본어 URL, 제목, 실행 문구, canonical, hreflang
- sitemap.xml 및 robots.txt의 007 등록
- 001 기준 하단 실행 버튼 클래스와 데스크톱 높이 일치
- 모바일 버튼이 작업장 안에 있고 세로 배치되는지
- 업로드, 자동 분석, 전체 최적화, 비교, ZIP 활성화, 초기화
- 투명 PNG의 JPG 강제 변환 방지
- 개별 설정 유지 및 전체 제외 시 실행 차단
- 미지원·빈 파일 거부
- 파일 수, 개별 용량, 전체 용량 경계값
- 중복 제거
- 최대 파일 수 일괄 처리 및 ZIP 활성화
