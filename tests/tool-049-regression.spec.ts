import {expect,test} from '@playwright/test';for(const locale of ['ko','en','ja'] as const){test(`TOOL049 ${locale} keeps related completed tools`,async({page})=>{await page.goto(`/${locale}/employment-tenure-calculator`);await expect(page.locator(`a[href="/${locale}/date-difference-calculator"]`)).toBeVisible();await expect(page.locator(`a[href="/${locale}/date-add-subtract-calculator"]`)).toBeVisible();});}
