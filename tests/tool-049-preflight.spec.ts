import {expect,test} from '@playwright/test';for(const locale of ['ko','en','ja'] as const){test(`TOOL049 ${locale} mount`,async({page})=>{await page.goto(`/${locale}/employment-tenure-calculator`);await expect(page.getByTestId('tool049-root')).toBeVisible();await expect(page.getByTestId('tool049-workspace')).toBeVisible();await expect(page.getByTestId('tool049-start')).toHaveValue('');await expect(page.getByTestId('tool049-current')).not.toBeChecked();await expect(page.getByTestId('tool049-result')).toHaveCount(0);});}
