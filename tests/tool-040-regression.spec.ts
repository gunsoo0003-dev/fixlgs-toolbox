import { test, expect } from '@playwright/test';
for(const locale of ['ko','en','ja'] as const){test(`TOOL040 ${locale} no horizontal overflow`,async({page})=>{await page.goto(`/${locale}/delimiter-list-converter`);await expect(page.getByTestId('tool040-root')).toBeVisible();const overflow=await page.evaluate(()=>document.documentElement.scrollWidth-document.documentElement.clientWidth);expect(overflow).toBeLessThanOrEqual(1);});}
