import {expect,test} from '@playwright/test';
test('TOOL055 dimension tabs replace unit registry',async({page})=>{await page.goto('/ko/length-area-volume-converter');await expect(page.getByTestId('tool055-from').locator('option[value="m"]')).toHaveCount(1);await page.getByTestId('tool055-tab-area').click();await expect(page.getByTestId('tool055-from').locator('option[value="m"]')).toHaveCount(0);await expect(page.getByTestId('tool055-from').locator('option[value="m2"]')).toHaveCount(1);await page.getByTestId('tool055-tab-volume').click();await expect(page.getByTestId('tool055-from').locator('option[value="m2"]')).toHaveCount(0);await expect(page.getByTestId('tool055-from').locator('option[value="l"]')).toHaveCount(1);});
