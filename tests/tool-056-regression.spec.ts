import {expect,test} from '@playwright/test';
test('TOOL056 uses common detail sections without global overrides',async({page})=>{await page.goto('/ko/weight-temperature-pressure-converter');await expect(page.locator('.toolbox-tool-guide')).toBeVisible();await expect(page.locator('.toolbox-tool-expert-post')).toBeVisible();await expect(page.locator('.toolbox-tool-info-band')).toBeVisible();await expect(page.locator('.toolbox-tool-faq')).toBeVisible();await expect(page.getByRole('link',{name:/단위·일반 계산기/})).toHaveAttribute('href','/ko/category/unit-calc');});
test('TOOL055 remains reachable from TOOL056 related tools',async({page})=>{await page.goto('/en/weight-temperature-pressure-converter');const link=page.getByRole('link',{name:/Length, Area & Volume Converter/});await expect(link).toHaveAttribute('href','/en/length-area-volume-converter');});
