import { expect, test } from '@playwright/test';
test('city add remove and 12/24 display controls remain interactive',async({page})=>{await page.goto('/en/world-time-timezone-converter');await page.getByTestId('tool052-city-search').fill('Dubai');await page.getByTestId('tool052-search-results').getByRole('button',{name:/Dubai/}).click();await expect(page.getByTestId('tool052-city-dubai')).toBeVisible();await page.getByRole('tab',{name:'12-hour'}).click();await expect(page.getByTestId('tool052-results')).toContainText(/AM|PM/);});
test('meeting planner renders common-time state',async({page})=>{await page.goto('/en/world-time-timezone-converter');await page.getByRole('tab',{name:'Common meeting time'}).click();await expect(page.getByTestId('tool052-meeting')).toBeVisible();});
