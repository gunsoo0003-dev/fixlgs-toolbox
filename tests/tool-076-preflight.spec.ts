import {expect,test} from '@playwright/test';for(const locale of ['ko','en','ja'])test(`TOOL076 preflight ${locale}`,async({page})=>{await page.goto(`/${locale}/credit-card-installment-calculator`);await expect(page.getByTestId('tool076-root')).toBeVisible();await expect(page.getByRole('heading',{level:1})).toBeVisible();await expect(page.getByTestId('tool076-fee-definition')).toBeVisible()});
