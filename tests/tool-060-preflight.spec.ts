import {test,expect} from '@playwright/test';
for(const locale of ['ko','en','ja'] as const)test(`TOOL060 ${locale} route/root contract`,async({page})=>{const errors:string[]=[];page.on('pageerror',e=>errors.push(e.message));page.on('console',m=>{if(m.type()==='error')errors.push(m.text())});await page.goto(`/${locale}/shoe-clothing-size-converter`);await expect(page.getByTestId('tool060-root')).toBeVisible();await expect(page.getByTestId('tool060-system')).toBeVisible();await expect(page.getByTestId('tool060-size')).toBeVisible();await expect(page.getByTestId('tool060-result')).toBeVisible();expect(errors).toEqual([])});
