import {test,expect} from '@playwright/test';
test('TOOL075 schedule shows 12 rows by default and expands to the last row',async({page})=>{await page.goto('/ko/loan-interest-calculator');await expect(page.getByTestId('tool075-compare')).toBeVisible();await expect(page.getByTestId('tool075-row-first')).toBeVisible();await expect(page.getByTestId('tool075-row-last')).toHaveCount(0);const toggle=page.getByTestId('tool075-schedule-toggle');await expect(toggle).toBeVisible();await expect(toggle).toHaveAttribute('aria-expanded','false');await toggle.click();await expect(toggle).toHaveAttribute('aria-expanded','true');await expect(page.getByTestId('tool075-row-last')).toBeVisible();});
