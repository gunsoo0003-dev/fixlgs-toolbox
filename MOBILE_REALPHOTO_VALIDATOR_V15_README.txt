FIXLGS TOOLBOX MOBILE REAL-PHOTO VALIDATOR V15

Stable input path:
  Photo & video -> Gallery -> Camera -> first photo

Rules:
- Do not use Recent items.
- Single-image tools: select Camera first photo once.
- Multi-image tools: repeat the same Camera first photo for every required input slot.
- After each selection returns to Chrome, immediately perform one small downward scroll.
- Keep full E2E workflow and actual download verification from V14.
- TOOL003 remains a source-format exception (SVG/BMP/TIFF input policy).
