import type {Metadata} from 'next';import {notFound} from 'next/navigation';import {locales,type Locale} from '@/lib/site';import {Tool059PixelPrintConverterPage} from '@/components/tool-059-pixel-print-converter-page';
const meta={ko:{title:'픽셀·인쇄 크기 변환기 | FIXLGS TOOLBOX',description:'픽셀 크기와 PPI/DPI로 인쇄 크기를 계산하고 필요한 픽셀과 실제 PPI를 확인합니다.'},en:{title:'Pixel & Print Size Converter | FIXLGS TOOLBOX',description:'Calculate print size from pixels and PPI, find required pixels, and check effective PPI.'},ja:{title:'ピクセル・印刷サイズ変換ツール | FIXLGS TOOLBOX',description:'ピクセルとPPI/DPIから印刷サイズを計算し、必要ピクセルと実効PPIを確認できます。'}} as const;
export async function generateMetadata({params}:{params:Promise<{locale:string}>}):Promise<Metadata>{const {locale}=await params;if(!locales.includes(locale as Locale))return{};const l=locale as Locale,path=`/${l}/pixel-print-size-converter`;return{title:meta[l].title,description:meta[l].description,alternates:{canonical:path,languages:{ko:'/ko/pixel-print-size-converter',en:'/en/pixel-print-size-converter',ja:'/ja/pixel-print-size-converter','x-default':'/ko/pixel-print-size-converter'}}}}
export default async function Page({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!locales.includes(locale as Locale))notFound();return <Tool059PixelPrintConverterPage locale={locale as Locale}/>}
