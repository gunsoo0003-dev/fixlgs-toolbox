# 006 전달서 미구현 기능 보강

## 이번 보강
- 가로 또는 세로 어느 쪽이든 기준값으로 선택해 비율 자동 계산
- 전체 처리 진행률과 처리 개수 표시
- 파일별 개별 설정 적용 상태 표시
- 예상 결과의 픽셀 수 변화, 변화 비율, 확대 상태 표시
- ZIP 생성 중 상태, 실패 안내, 결과 보존, 재시도
- 다운로드 실패 안내
- 파일 선택 오류 문구 한국어·영어·일본어 처리 및 다음 행동 안내
- 동일 선택 묶음 안의 중복 파일 차단
- 사진/자동 추천은 고품질 스무딩, 그래픽·아이콘은 중간 스무딩, 픽셀아트는 스무딩 해제
- 처리 중 페이지 이탈 경고
- 처리 취소 시 현재 파일 결과도 확정하지 않고 취소 상태로 전환
- 확대 방지는 한 축이라도 원본보다 커지는 설정을 원본 유지로 처리
- 처리 전 상태를 `크기 계산 완료`로 표시
- 006 Playwright 검수 항목 추가

## 안전선 상태
코드의 파일 수·용량·픽셀 제한은 확정값이 아니라 브라우저 보호를 위한 임시 안전선이다. 실제 PC·모바일 부하검수 후 사용자 확인을 거쳐 확정해야 한다.

## 기술 검증 후 유지되는 제한
- 크기가 변경된 JPG·PNG·WebP의 메타데이터 완전 보존은 현재 Canvas 재인코딩 방식에서 보장하지 않는다. UI는 원본 크기 유지 파일에서만 메타데이터가 보존된다고 명시한다.
- 실제 인코딩 내부 진행률은 브라우저 Canvas API가 제공하지 않아 파일 단위 전체 진행률로 표시한다.
- 현재 파일 디코딩·인코딩을 즉시 중단하는 Worker 기반 강제 취소는 포함하지 않는다. 취소 요청 후 생성된 결과를 폐기하고 다음 파일을 처리하지 않는다.
