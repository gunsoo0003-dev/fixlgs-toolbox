# 006 부하·안정성 한계검수기 재작성

- 비율 유지·확대 방지 체크박스에 전용 `data-testid`를 추가했다.
- 텍스트 라벨 선택자 대신 `global-keep-ratio`, `global-prevent-upscale`를 사용한다.
- 4MP·12MP·24MP·36MP 데스크톱 단계를 각각 독립 테스트로 분리했다.
- 4MP·8MP·12MP·16MP 모바일 viewport 단계를 각각 독립 테스트로 분리했다.
- 한 단계가 실패해도 다음 단계가 건너뛰어지지 않도록 serial 묶음을 제거했다.
- 각 단계에 별도 제한시간을 적용했다.
- 단계별 처리시간·실제 결과 크기·상태를 `test-results/006-load-limit-metrics`에 JSON으로 저장한다.
- 모바일 결과는 실제 기기 메모리 한계가 아닌 테스트 PC의 viewport 에뮬레이션임을 명시한다.
- 4천만 픽셀을 넘는 48MP 출력은 현재 임시 보호값에 의해 실행 전에 차단되는지 별도로 검수한다.
