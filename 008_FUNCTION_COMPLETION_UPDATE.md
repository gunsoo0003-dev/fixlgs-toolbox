# 008 기능 완성 보완

- 동일 파일 SHA-256 중복 제거
- 애니메이션 WebP·APNG 시그니처 차단
- 모바일 두 손가락 핀치 줌 및 PC 마우스 휠 줌
- 모서리 4개 + 변 4개 자르기 핸들
- 정밀 좌표는 입력 후 `좌표 적용` 버튼으로 반영
- 미세 회전 숫자 입력은 `각도 적용` 버튼으로 반영
- 회전 후 실제 캔버스 기준 결과 픽셀 표시
- 선택 파일 일괄 적용 대상 추가
- 일괄 비율 적용 시 좌표 복사가 아니라 비율만 적용
- 건너뛴 파일의 `원본으로 포함` 선택과 ZIP 포함
- ZIP 동일 파일명 충돌 방지
- 결과 재생성·일괄 재편집 시 이전 Object URL 해제
- 실행 취소 기록 최대 30단계

AVIF와 메타데이터 유지는 확정된 1차 제외 범위다.
