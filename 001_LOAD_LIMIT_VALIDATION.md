# 001 이미지 변환기 부하·한계·경계 검수

## 목적
001 JPG·PNG·WebP 이미지 변환기의 기존 제한값은 실제 단계형 한계검수 없이 설정된 임시 보호값이다. 이 검수기는 006에서 검증한 독립 단계형 구조를 001 변환 특성에 맞게 이식한다.

## 실행

```powershell
npm run test:toolbox:001-load
```

## 검수 범위
- 데스크톱: 4MP, 12MP, 24MP, 36MP, 40MP
- 모바일 viewport: 4MP, 8MP, 12MP, 16MP
- JPG·PNG·WebP 출력 인코딩을 단계별로 교차 검증
- 10개 허용 / 11개 차단
- 40MP 허용 / 40,005,000px 차단
- 파일당 20MB 허용 / 초과 차단
- 전체 60MB 허용 / 초과 차단
- 각 단계 처리시간과 상태를 `test-results/001-load-limit-metrics`에 JSON으로 기록

## 현재 상태
검수 실행 전이므로 다음 값은 아직 확정 운영 안전선이 아니다.
- 최대 파일 수 10개
- 파일당 20MB
- 전체 60MB
- 파일당 40MP

검수 결과를 확인한 뒤 통과값보다 보수적인 운영 안전선을 확정하고 코드·다국어 문구에 반영한다.

## 주의
모바일 프로젝트 결과는 테스트 PC에서 모바일 viewport를 에뮬레이션한 결과다. 실제 모바일 기기의 메모리 한계를 직접 측정한 결과는 아니다.
