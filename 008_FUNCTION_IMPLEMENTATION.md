# 008 기능 구현

- 브라우저 로컬 JPG/JPEG/PNG/WebP 입력
- 다중 파일, 드래그앤드롭, 파일별 상태
- 자유/원본/1:1/4:3/3:2/16:9/사용자 비율
- 자르기 영역 이동·모서리 조절, 이미지 이동, 줌
- 90도/미세 회전, 좌우·상하 반전
- Undo/Redo(파일별 최대 30단계), 편집 초기화
- 정밀 좌표 입력, 결과 픽셀 표시
- 적용하고 다음, 건너뛰기, 결과 재편집
- 원본 형식 유지, JPG/WebP 품질, PNG 무손실
- 개별 다운로드, ZIP 다운로드
- 일괄 비율/90도 회전
- 안전 한도: 10개, 파일당 15MB, 전체 50MB, 파일당 16,986,931픽셀, 최대 한 변 16384px
- AVIF와 메타데이터 유지는 확정안대로 1차 제외

## 2026-08-06 기능 완성 보완

이번 보완에서는 전달서의 비용 없는 로컬 기능 중 기존 구현에서 빠졌거나 잘못 연결된 부분을 추가했다.

- 파일 내용 SHA-256 중복 제거
- 애니메이션 WebP와 APNG 시그니처 차단
- PC 마우스 휠 줌
- 모바일 두 손가락 핀치 줌
- 모서리뿐 아니라 네 변 자르기 핸들
- 정밀 좌표 입력 후 명확한 `좌표 적용` 버튼
- 미세 회전 숫자 입력 후 명확한 `각도 적용` 버튼
- 선택 파일 일괄 적용 대상
- 일괄 비율 적용 시 파일별 위치를 복사하지 않고 비율만 적용
- 건너뛴 파일의 원본 포함 선택
- 원본 포함 파일의 ZIP 추가
- ZIP 동일 파일명 충돌 방지
- 회전 상태를 반영한 결과 픽셀 표시
- 실행 취소 기록 최대 30단계
- 결과 재생성 시 이전 Object URL 해제

AVIF와 메타데이터 유지 옵션은 확정된 1차 제외 범위다. 자동 한계탐색·안전한도 확정과 실기기 모바일 검수는 기능 구현 뒤 별도 통합검수 단계에서 진행한다.
