TOOL001 REAL PHOTO V28

Purpose
- Remove diagnostic automation of Android Photo Picker commit controls.
- Use the same natural user flow as a real competitor/service:
  open native picker -> user taps one photo -> if picker stays open, user taps OS Done/Add/Select once.
- Harness only observes:
  picker selection -> picker close -> Chrome return -> input.files/change -> provider read -> validation -> preview.
- Keep broad 8-case GOOD/BAD root-cause sweep.
- Product code is unchanged.

Why
- V27 reached real selection but failed at COMMIT_CONTROL_NOT_EXPOSED because the harness itself required and tried to automate the OS commit control.
- V28 removes that diagnostic bottleneck and measures the real product path.

Run
node scripts/verify-tool-001-real-photo-diagnostic-v28.mjs
node scripts/run-tool-001-real-photo-diagnostic-v28.mjs --url "https://toolbox.fixlgs.com/ko/jpg-png-webp-image-converter"
