TOOL001 REAL ANDROID FINAL CAPTURE PATCH - 2026-08-12

Basis
- V14 real Galaxy result confirmed Photo Picker open/select/commit and trusted input change.
- V14 deep probe was invasive: its capture-phase change listener read the same Android provider-backed File before/while the product also tried to capture it. This can create/accelerate NotReadableError / ERR_UPLOAD_FILE_CHANGED and contaminate root-cause judgment.
- Latest product source already had capturePickerFile(), but picker files were placed behind attachmentQueueRef before capture began. A previous pending selection could therefore delay the first provider read.

Product fix
- components/image-converter-tool.tsx
- Native picker onChange now starts capturePickerFile() immediately for all eligible selected files in the same change turn.
- First provider reads begin before attachmentQueueRef waiting.
- Multiple picker files capture concurrently via Promise.allSettled.
- After capture, only app-owned File objects enter the existing validation/preview/conversion queue.
- Input value remains uncleared until immediate capture + queued attach processing finishes.
- Drag/drop path remains unchanged.

Validator fix
- scripts/run-tool-001-real-android-deep-v15.mjs
- V15 never reads the original File from its change listener.
- No slice/arrayBuffer/FileReader/objectURL/createImageBitmap on the native provider File.
- It observes trusted change + actual TOOL001 file-card/preview creation instead.
- Android 15 Photo Picker precise-selection/commit logic from V14 is retained.

Static verification
- node --check V15 runner: PASS
- V15 verifier: STATIC_SELFTEST=PASS
- Product capture verifier: STATIC_PRODUCT_SELFTEST=PASS
- TOOL001 mobile workflow coverage: 72 risks, missing=0

Important
- Full Next.js build was not run in the packaging environment because the uploaded source ZIP intentionally excludes node_modules. TypeScript syntax parsing was checked with global tsc; only unresolved dependency/import errors remained.
