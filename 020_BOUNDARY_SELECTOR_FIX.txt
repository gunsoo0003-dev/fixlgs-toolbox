TOOL 020 boundary validator selector fix

Reason:
- Initial-state validation errors are rendered as role=alert but do not carry data-testid=tool020-error.
- The failed screenshots/error-context show the expected error messages were actually rendered.
- Therefore the three boundary failures were validator false negatives, not product failures.

Change:
- Boundary tests now assert the initial-state accessible alert via root.getByRole('alert').
- Product component/source is not modified.
