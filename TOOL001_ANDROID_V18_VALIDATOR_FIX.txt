TOOL001 REAL ANDROID DEEP V18 - Photo Picker target lock / exact size proof

Product code:
- V17 MB-size capture engine retained unchanged.
- 20MB per-file / 60MB total safety limits retained.

Validator fixes:
1. Device screenshots use `adb exec-out screencap -p` directly to PC output.
   No diagnostic screenshot is written into Pictures/MediaStore.
2. MediaStore target must be newest after staging.
3. Target is re-verified immediately before picker activation.
4. Target is re-verified again immediately before tapping Photo Picker's first grid cell.
5. Browser change File.size must exactly equal the generated target file bytes.
   Any mismatch => WRONG_MEDIA_SELECTED and that attempt can never PASS.
6. Default size sweep remains 0.5MB / 3MB / 8MB / 15MB / 19MB.
7. Existing Photo Picker drag-handle guard, commit verification, non-invasive browser observer,
   heartbeat, timeout and Desktop result ZIP behavior are retained.
