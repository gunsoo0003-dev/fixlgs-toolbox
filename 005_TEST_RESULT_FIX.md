# 005 검수 결과 수정

## 기능 검수 결과 해석

- 1200×900 노이즈 JPG는 최소 품질 40과 원본 픽셀 유지 조건에서 100KB 달성이 불가능했고, 도구가 `목표 달성 불가`를 반환한 것은 정상 동작이었다.
- 동일한 이유로 1200×900 WebP의 100KB 목표와 파일별 80KB/120KB 목표도 검수 기대값이 잘못 설정되어 있었다.
- 부하 검수의 200KB 목표 역시 같은 샘플과 최소 품질 조건에서 달성 불가능했다.
- 고급 설정의 최소 품질 슬라이더는 접힌 `details` 내부에 있어 테스트가 먼저 열어야 한다.
- EXIF 샘플은 3.1KB이므로 20KB 목표에서는 원본 유지(`already`)가 정상이다. 방향 보정을 실제 재인코딩으로 검증하도록 2KB 목표로 변경했다.

## 변경 기준

- JPG 기능 검수: 400KB 목표
- WebP 기능 검수: 500KB 목표
- 파일별 목표: JPG 400KB / WebP 500KB
- 부하 검수: 500KB 목표
- 고급 설정: 펼친 뒤 최소 품질 변경
- EXIF 검수: 2KB 목표로 재인코딩 강제

이 변경은 압축 실패를 숨기기 위한 완화가 아니라, `원본 크기 유지 + 최소 품질 40` 조건에서 실제로 달성 가능한 목표와 달성 불가능한 목표를 분리하여 검증하기 위한 수정이다.
