# 007 완료 전 이중 재점검 기록

## 작업 시작 전 확인

1. 간단한 1차 수준에 머물렀는가?
   - 기존 구현은 기능 범위는 넓었지만 디자인 이식 기준을 통과하지 못해 완료 불가로 판정했다.
2. 기준 페이지 디자인을 실제로 이식했는가?
   - 기존 구현은 일부 클래스만 재사용했으므로 미흡으로 판정했다.
3. 이전 반복 실수를 다시 했는가?
   - 새 작업영역을 별도로 구성하고도 이식 완료라고 판단한 실수를 확인했다.
4. 비용이 발생하지 않으며 안전하게 구현 가능한 기능 중 누락이 있는가?
   - 비교, 후보 품질 검사, 파일별 재최적화 등은 구현되어 있었으나 디자인 이식과 검수 깊이가 부족했다.

## 수정 내용

- 006의 검증된 작업영역 DOM 흐름을 007에 그대로 적용했다.
- `toolbox-tool-workflow → toolbox-workbench/target-size-workbench/resizer-workbench → toolbox-workbench-upload → target-size-workbench-files → actions → summary` 순서를 유지했다.
- 업로드 빈 상태, 업로드 후 축소 상태, 설정 헤더, 파일 카드, 예상 결과, 진행률, 실행 버튼, 결과 요약 구조를 006과 같은 클래스 및 배치로 이식했다.
- 007 전용 기능은 자동 추천 설정, 유형 분석, 후보 상세, 자동 선택 이유, 비교 화면으로만 추가했다.
- 디자인 이식 여부를 자동검수하는 구조 검사를 007 Playwright 테스트에 추가했다.

## 완료 직전 확인

1. 간단한 1차 수준인가?
   - 아니오. 실제 후보 생성, 품질·색상·경계·투명도 검사, 원본 유지, 파일별 설정과 재처리, 비교 슬라이더, ZIP을 포함한다.
2. 기준 페이지 디자인을 실제로 이식했는가?
   - 예. 006 작업영역의 검증된 최상위 DOM 순서와 핵심 클래스 구조를 그대로 사용하고 007 전용 기능만 추가했다.
3. 이전 반복 실수를 다시 했는가?
   - 새 독립 작업영역을 만들지 않았고, 완료 전 코드 구조와 자동검수 항목으로 다시 대조했다.
4. 비용 없는 안전한 기능 중 미구현 항목이 남았는가?
   - 제작서의 브라우저 로컬 핵심 기능은 반영했다. 메타데이터 완전 유지 옵션은 기존 공통 정책에 따라 제외했다.

## 실행 검수 상태

- TypeScript/TSX 단일 파일 정적 타입·구문 검사: 통과
- npm install / Playwright / Next.js build: 현재 실행 환경의 내부 npm 저장소에 `@playwright/test`가 없어 실행 불가
- 로컬 PC에서 실제 기능·부하·경계·회귀검수 및 빌드를 수행한 뒤 운영 안전선을 확정해야 한다.
