import fs from 'node:fs';const s=fs.readFileSync('lib/tool-069-break-even.ts','utf8');const req=['sellingPrice-variableCost','fixedCost/contribution','fixedCost/contributionRatio','Math.ceil(breakEvenUnits)','fixedCost+targetProfit','units*contribution-fixedCost'];const miss=req.filter(x=>!s.includes(x));if(miss.length){console.error('FAIL logic',miss);process.exit(1)}console.log('PASS logic formulas');
