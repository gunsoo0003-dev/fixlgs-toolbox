import fs from 'node:fs';const c=JSON.parse(fs.readFileSync('tests/fixtures/tool-069/cases.json','utf8'));const b=c.base;const cm=b.selling-b.variable;const ratio=cm/b.selling;const be=b.fixed/cm;const rev=b.fixed/ratio;const ok=cm===b.contribution&&ratio===b.ratio&&be===b.beUnits&&Math.ceil(be)===b.practical&&rev===b.beRevenue&&((b.fixed+c.target.profit)/cm)===c.target.units&&((c.volume.units*cm-b.fixed)===c.volume.profit);const d=c.decimal;const dbe=d.fixed/(d.selling-d.variable);if(!ok||Math.abs(dbe-d.beUnits)>1e-12||Math.ceil(dbe)!==d.practical){console.error('FAIL fixtures');process.exit(1)}console.log('PASS fixture economics: base/target/volume/decimal');
