import fs from 'node:fs';
const source=fs.readFileSync('lib/tool-053-unix-timestamp.ts','utf8'); const product=fs.readFileSync('components/tool-053-unix-timestamp-tool.tsx','utf8');
let pass=0,fail=0;const c=(n,v)=>{console.log(`${v?'PASS':'FAIL'} ${n}`);v?pass++:fail++};const eq=(n,a,b)=>c(n,Object.is(a,b));
const convert=(value,unit)=>{const ms=unit==='seconds'?value*1000:value;if(!Number.isSafeInteger(ms)||ms < -8640000000000000 || ms > 8640000000000000)throw new RangeError();return new Date(ms).toISOString()};
const hint=(value,unit)=>{const s=value.trim().replace(/^-/,'');if(!/^\d+$/.test(s))return null;if(unit==='seconds'&&s.length>=13)return'likely-milliseconds';if(unit==='milliseconds'&&s.length===10)return'likely-seconds';return null};
eq('epoch',convert(0,'seconds'),'1970-01-01T00:00:00.000Z');eq('Y2K seconds',convert(946684800,'seconds'),'2000-01-01T00:00:00.000Z');eq('Y2K milliseconds',convert(946684800000,'milliseconds'),'2000-01-01T00:00:00.000Z');eq('2024',convert(1704067200,'seconds'),'2024-01-01T00:00:00.000Z');eq('negative',convert(-1,'seconds'),'1969-12-31T23:59:59.000Z');eq('13-digit hint',hint('1704067200000','seconds'),'likely-milliseconds');eq('10-digit hint',hint('1704067200','milliseconds'),'likely-seconds');eq('selected unit preserved by design',product.includes('unitMismatchHint(timestamp,unit)')&&product.includes('setUnit("seconds")')&&product.includes('setUnit("milliseconds")'),true);c('source JS Date max constant',source.includes('8640000000000000'));c('source integer-only',source.includes('/^-?\\d+$/'));c('source invalid date roundtrip',source.includes('date.getFullYear() !== year')&&source.includes('date.getDate() !== day'));c('source local Date constructor',source.includes('new Date(year, month - 1, day, hour, minute, second, 0)'));c('source seconds-ms relation',source.includes('Math.trunc(milliseconds / 1000)'));let overflow=false;try{convert(8640000000001,'seconds')}catch{overflow=true}c('range overflow',overflow);
console.log(`RESULT LOGIC-CROSSCHECK PASS=${pass} FAIL=${fail}`);if(fail)process.exit(1);
