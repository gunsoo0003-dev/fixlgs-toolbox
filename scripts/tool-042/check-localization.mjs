import fs from 'node:fs';const files=['components/text-find-replace-tool.tsx','components/text-find-replace-page.tsx','app/[locale]/text-find-replace/page.tsx'];let f=0;for(const file of files){const s=fs.readFileSync(file,'utf8');for(const locale of ['ko','en','ja']){const ok=new RegExp(`${locale}\\s*:`).test(s)||s.includes(`locale===\"${locale}\"`);console.log(`[${ok?'PASS':'FAIL'}] ${file} ${locale}`);if(!ok)f++}}process.exitCode=f?1:0;
