import fs from 'node:fs';let pass=0,fail=0;const c=(n,v)=>{console.log(`${v?'PASS':'FAIL'} ${n}`);v?pass++:fail++;};for(const s of ["boundary", "change", "core", "design", "feature", "limit", "preflight", "regression", "roundtrip"]){const f=`tests/tool-061-${s}.spec.ts`;c(`spec ${s}`,fs.existsSync(f));if(fs.existsSync(f)){const t=fs.readFileSync(f,'utf8');c(`spec ${s} runtime`,/\btest\s*\(/.test(t));c(`spec ${s} no skip-fixme-only`,!/(test|describe)\.(skip|fixme|only)\s*\(/.test(t));}}c('config',fs.existsSync('playwright.tool061.config.ts'));c('runner',fs.existsSync('scripts/tool-061/run-validation-full.mjs'));c('secret',fs.existsSync('scripts/tool-061/check-secret-scan.mjs'));console.log(`RESULT HARNESS PASS=${pass} FAIL=${fail}`);if(fail)process.exit(1);
