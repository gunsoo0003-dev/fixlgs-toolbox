import fs from 'node:fs';let pass=0,fail=0;const check=(n,o)=>{console.log(`${o?'PASS':'FAIL'} ${n}`);o?pass++:fail++};const comp=fs.readFileSync('components/tool-090-stair-ramp-calculator.tsx','utf8');const fixture=JSON.parse(fs.readFileSync('tests/fixtures/tool-090/cases.json','utf8'));
for(const s of ['tool090-root','tool090-workspace','tool090-height','tool090-steps','tool090-stair-basis','tool090-ramp-basis','tool090-result','tool090-error'])check(`selector ${s}`,comp.includes(s));check('fixture stair cases',fixture.stair.length>=2);check('fixture ramp cases',fixture.ramp.length>=3);check('fixture zero step/run',fixture.errors.some(x=>x.name==='zero step')&&fixture.errors.some(x=>x.name==='zero run'));check('fixture limits',fixture.limits.steps===1000&&fixture.limits.angleDeg===89.999);console.log(`RESULT HARNESS PASS=${pass} FAIL=${fail}`);process.exitCode=fail?1:0;
