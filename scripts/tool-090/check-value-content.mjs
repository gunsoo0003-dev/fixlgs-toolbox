import fs from 'node:fs';let pass=0,fail=0;const check=(n,o)=>{console.log(`${o?'PASS':'FAIL'} ${n}`);o?pass++:fail++};const lib=fs.readFileSync('lib/tool-090-stair-ramp.ts','utf8'),page=fs.readFileSync('components/tool-090-stair-ramp-calculator-page.tsx','utf8');for(const x of ['maxLengthM:1e6','maxSteps:1000','maxTreadM:10','maxSlopePct:1000','maxAngleDeg:89.999','maxPrecision:8','maxInputChars:30'])check(`limit ${x}`,lib.includes(x));for(const x of ['2m','4m','50%','26.565','4.472','2.4','12','0.20','0.28','3.36','10%'])check(`content ${x}`,page.includes(x));check('legal exclusion KO',page.includes('법정·구조 기준 적합 여부는 판정하지 않습니다'));check('legal exclusion EN',page.includes('provides geometry calculations only'));check('legal exclusion JA',page.includes('幾何計算のみ'));console.log(`RESULT VALUE PASS=${pass} FAIL=${fail}`);process.exitCode=fail?1:0;
