import fs from 'node:fs';const files=['lib/tool-068-seller-fee.ts','components/tool-068-seller-fee-settlement-calculator.tsx','components/tool-068-seller-fee-settlement-calculator-page.tsx'];const patterns=[/api[_-]?key\s*[:=]/i,/secret\s*[:=]/i,/Bearer\s+[A-Za-z0-9._-]+/];const hits=[];for(const f of files){const s=fs.readFileSync(f,'utf8');for(const p of patterns)if(p.test(s))hits.push(`${f}:${p}`)}console.log(JSON.stringify({check:'TOOL068 secret scan',hits},null,2));process.exitCode=hits.length?1:0;
