import fs from 'node:fs';const specs=['preflight','core','feature','shipping','reverse','boundary','limit','design','regression'];const fails=[];for(const s of specs){const p=`tests/tool-068-${s}.spec.ts`;if(!fs.existsSync(p))fails.push(p)}const fx=JSON.parse(fs.readFileSync('tests/fixtures/tool-068/cases.json','utf8'));if(fx.core.find(x=>x.id==='profit')?.netProfit!==35000)fails.push('fixture-profit');if(fx.core.find(x=>x.id==='mixed-fee')?.settlement!==93700)fails.push('fixture-mixed');console.log(JSON.stringify({check:'TOOL068 harness structure',specs:specs.length,fails},null,2));process.exitCode=fails.length?1:0;
