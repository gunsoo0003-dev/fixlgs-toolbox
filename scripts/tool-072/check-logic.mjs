import fs from 'node:fs';let pass=0,fail=0;const c=(n,o)=>{console.log(`${o?'PASS':'FAIL'} ${n}`);o?pass++:fail++};const e=fs.readFileSync('lib/tool-072-salary.ts','utf8');c('hourly daily formula',e.includes('daily=hourly*p.hoursPerDay'));c('daily monthly formula',e.includes('monthly=daily*p.workdaysPerMonth'));c('monthly hourly paid-hours divisor',e.includes('hourly=monthly/r.monthlyPaidHours'));c('annual hourly paid-hours divisor',e.includes('hourly=annual/r.annualPaidHours'));c('direct monthly override',e.includes('p.monthlyPaidHours==null?derivedMonthly:p.monthlyPaidHours'));c('direct annual override',e.includes('p.annualPaidHours==null?derivedAnnual:p.annualPaidHours'));console.log(`RESULT LOGIC PASS=${pass} FAIL=${fail}`);process.exitCode=fail?1:0;
