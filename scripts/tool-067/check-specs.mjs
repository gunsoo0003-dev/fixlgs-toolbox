import fs from 'node:fs';let pass=0,fail=0;const c=(n,v)=>{console.log(`${v?'PASS':'FAIL'} ${n}`);v?pass++:fail++};const names=['preflight','core','feature','margin','markup','target','boundary','roundtrip','regression','limit','design'];for(const n of names){const f=`tests/tool-067-${n}.spec.ts`;c(`spec exists ${n}`,fs.existsSync(f));if(fs.existsSync(f)){const s=fs.readFileSync(f,'utf8');c(`spec id 067 ${n}`,s.includes('tool-067')||s.includes('selling-price-margin-calculator'));}}const all=names.map(n=>fs.readFileSync(`tests/tool-067-${n}.spec.ts`,'utf8')).join('\n');for(const s of ['tool-067-profit','tool-067-margin','tool-067-markup','tool-067-target-price','tool-067-allowed-cost'])c(`assert target ${s}`,all.includes(s));c('locale coverage',all.includes("['ko','en','ja']"));console.log(`RESULT SPECS PASS=${pass} FAIL=${fail}`);if(fail)process.exit(1);
