import fs from 'node:fs';let pass=0,fail=0;const c=(n,v)=>{console.log(`${v?'PASS':'FAIL'} ${n}`);v?pass++:fail++};const product=fs.readFileSync('components/tool-067-selling-price-margin-calculator.tsx','utf8');const fixtures=JSON.parse(fs.readFileSync('tests/fixtures/tool-067/cases.json','utf8'));for(const s of ['tool-067-root','tool-067-cost','tool-067-selling','tool-067-target-margin','tool-067-result','tool-067-profit','tool-067-margin','tool-067-markup','tool-067-target-price','tool-067-allowed-cost'])c(`selector ${s}`,product.includes(s));c('mode selector template',product.includes('tool-067-mode-${m}')&&product.includes("['margin','target','allowed']"));c('core fixtures >=2',fixtures.core.length>=2);c('target fixtures 0/20/25/99', [0,20,25,99].every(n=>fixtures.target.some(x=>x.margin===n)));c('100 boundary fixture',fixtures.boundary.target100==='TARGET_MARGIN_RANGE');console.log(`RESULT HARNESS PASS=${pass} FAIL=${fail}`);if(fail)process.exit(1);
