import {spawnSync} from 'node:child_process';const steps=['check-logic.mjs','check-source.mjs','check-design.mjs'];let fail=0;for(const step of steps){console.log(`\n=== TOOL064 ${step} ===`);const r=spawnSync(process.execPath,['--experimental-strip-types',`scripts/tool-064/${step}`],{stdio:'inherit'});if(r.status!==0)fail++;}console.log(`\nTOOL064 STATIC FINAL FAIL=${fail} SKIP=0`);process.exit(fail?1:0);
