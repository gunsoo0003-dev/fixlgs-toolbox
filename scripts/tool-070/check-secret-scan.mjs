import fs from 'node:fs';import path from 'node:path';const roots=['components','lib','scripts/tool-070','tests'];const patterns=[/-----BEGIN (?:RSA |EC |OPENSSH )?PRIVATE KEY-----/i,/AKIA[0-9A-Z]{16}/,/AIza[0-9A-Za-z_-]{35}/,/sk-[A-Za-z0-9_-]{20,}/];let scanned=0;const findings=[];function walk(p){if(!fs.existsSync(p))return;for(const e of fs.readdirSync(p,{withFileTypes:true})){const f=path.join(p,e.name);if(e.isDirectory())walk(f);else{scanned++;let t='';try{t=fs.readFileSync(f,'utf8')}catch{continue}for(const re of patterns)if(re.test(t))findings.push(`${f}:${re}`)}}}for(const r of roots)walk(r);console.log(`RESULT SECRET_SCAN ${findings.length?'FAIL':'PASS'} scanned=${scanned} findings=${findings.length}`);process.exitCode=findings.length?1:0;
