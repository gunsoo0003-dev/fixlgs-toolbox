import fs from 'node:fs';const files=['components/text-extractor-tool.tsx','components/text-extractor-page.tsx','app/[locale]/text-extractor/page.tsx'];let f=0;for(const file of files){const s=fs.readFileSync(file,'utf8');for(const locale of ['ko','en','ja']){if(new RegExp(`${locale}:`).test(s)||new RegExp(`${locale}\\s*:`).test(s))console.log(`[PASS] ${file} ${locale}`);else{console.error(`[FAIL] ${file} missing ${locale}`);f++;}}}process.exitCode=f?1:0;
