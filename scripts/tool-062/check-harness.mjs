import fs from 'node:fs';let pass=0,fail=0;const c=(n,v)=>{console.log(`${v?'PASS':'FAIL'} ${n}`);v?pass++:fail++;};for(const s of ['preflight','core','feature','stacked','reverse','boundary','roundtrip','regression','limit','design']){const f=`tests/tool-062-${s}.spec.ts`;c(`spec ${s}`,fs.existsSync(f));if(fs.existsSync(f))c(`spec ${s} no skip/fixme/only`,!/(test|describe)\.(skip|fixme|only)\s*\(/.test(fs.readFileSync(f,'utf8')))}c('config',fs.existsSync('playwright.tool062.config.ts'));c('fixture',fs.existsSync('tests/fixtures/tool-062/cases.json'));c('runner',fs.existsSync('scripts/tool-062/run-validation-full.mjs'));console.log(`RESULT HARNESS PASS=${pass} FAIL=${fail}`);if(fail)process.exit(1);
