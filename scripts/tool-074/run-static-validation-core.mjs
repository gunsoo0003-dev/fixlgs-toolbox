import {spawnSync} from 'node:child_process';let fail=0;for(const s of ['check-source.mjs','check-logic.mjs','check-design.mjs','check-harness.mjs']){const p=spawnSync(process.execPath,[`scripts/tool-074/${s}`],{encoding:'utf8'});console.log(`\n### ${s}\n${p.stdout||''}`);if(p.stderr)process.stderr.write(p.stderr);if(p.status!==0)fail++}console.log(`\nTOOL074 STATIC VALIDATION: ${fail?'FAIL':'PASS'} (${4-fail}/4)`);process.exitCode=fail?1:0;
