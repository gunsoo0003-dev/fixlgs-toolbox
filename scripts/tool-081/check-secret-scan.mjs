import fs from 'node:fs';const files=['lib/tool-081-area-price.ts','components/tool-081-area-price-calculator.tsx','components/tool-081-area-price-calculator-page.tsx','app/[locale]/area-price-per-unit-calculator/page.tsx'];const text=files.map(f=>fs.readFileSync(f,'utf8')).join('\n');const patterns=[/AIza[0-9A-Za-z_-]{20,}/,/sk-[0-9A-Za-z]{20,}/,/BEGIN (?:RSA |EC )?PRIVATE KEY/];const hit=patterns.some(r=>r.test(text));console.log(`${hit?'FAIL':'PASS'} TOOL081 secret scan`);process.exitCode=hit?1:0;
