import {spawnSync} from 'node:child_process';let fail=0;for(const f of ['check-source.mjs','check-design.mjs','check-harness.mjs']){const r=spawnSync(process.execPath,[`scripts/tool-063/${f}`],{encoding:'utf8'});process.stdout.write((r.stdout||'')+(r.stderr||''));if(r.status!==0)fail++;}console.log(`TOOL063 STATIC FINAL ${fail?'FAIL':'PASS'} FAIL=${fail}`);if(fail)process.exit(1);
