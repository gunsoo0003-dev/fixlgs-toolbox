const LIMIT=1_000_000;
function expected(input,o){if(input.length>LIMIT)throw new RangeError('limit');let t=input.replace(/\r\n/g,'\n').replace(/\r/g,'\n');if(o.removeTabs)t=t.replace(/\t/g,'');let l=t.split('\n');if(o.trimEachLine)l=l.map(x=>x.replace(/^[ \t]+|[ \t]+$/g,''));if(o.collapseSpaces)l=l.map(x=>x.replace(/ {2,}/g,' '));if(o.removeBlankLines)l=l.filter(x=>x.length>0);let out=l.join('\n');if(o.eol==='crlf')out=out.replace(/\n/g,'\r\n');return out}
const d={collapseSpaces:true,trimEachLine:true,removeTabs:true,removeBlankLines:true,eol:'lf'};const cases=[['basic','  A   B  ','A B',d],['mixed','A\r\nB\rC\nD','A\nB\nC\nD',d],['tabs','A\tB','AB',d],['blank','A\n\n \nB','A\nB',d],['unicode','한글\u00a0EN\u3000日本語\u200b🙂','한글\u00a0EN\u3000日本語\u200b🙂',d],['crlf','A\nB','A\r\nB',{...d,eol:'crlf'}],['preserve blank','A\n\nB','A\n\nB',{...d,removeBlankLines:false}],['preserve tabs','A\tB','A\tB',{...d,removeTabs:false}]];let pass=0;for(const [n,i,e,o] of cases){const a=expected(i,o);if(a!==e){console.error('FAIL',n,JSON.stringify(a),JSON.stringify(e));process.exit(1)}pass++}if(expected('A'.repeat(LIMIT),d).length!==LIMIT)process.exit(1);try{expected('A'.repeat(LIMIT+1),d);process.exit(1)}catch{}console.log(`TOOL037 LOGIC FIXTURE PASS ${pass+2} / ${pass+2}`);
