import fs from 'node:fs';
const css=fs.readFileSync('components/tool-088-concrete-volume-calculator.module.css','utf8');const page=fs.readFileSync('components/tool-088-concrete-volume-calculator-page.tsx','utf8');let p=0,f=0;const ok=(n,c)=>{console.log((c?'PASS ':'FAIL ')+n);c?p++:f++};ok('module css scoped',css.includes('.root')&&css.includes('@media(max-width:560px)'));ok('primary action black',css.includes('background:#111')&&css.includes('color:#fff'));ok('mobile one-column inputs',css.includes('.dimensionGrid,.optionGrid,.resultGrid{grid-template-columns:1fr}'));ok('common shell reuse',page.includes('ToolboxSubpageShell'));ok('common guide reuse',page.includes('toolbox-tool-guide'));ok('FAQ reuse',page.includes('ToolboxFaqList'));ok('KO EN JA copy',page.includes("ko:{title:")&&page.includes("en:{title:")&&page.includes("ja:{title:"));ok('no global selector',!css.includes('toolbox-'));console.log(`DESIGN PASS=${p} FAIL=${f}`);process.exit(f?1:0);
