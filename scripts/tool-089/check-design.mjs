import fs from 'node:fs';let p=0,f=0;const t=(n,c)=>{console.log(c?'PASS':'FAIL',n);c?p++:f++};const css=fs.readFileSync('components/tool-089-roof-area-calculator.module.css','utf8');const page=fs.readFileSync('components/tool-089-roof-area-calculator-page.tsx','utf8');t('module css only',css.includes('.workspace')&&!css.includes(':global('));t('mobile breakpoint',css.includes('@media(max-width:560px)'));t('primary black action',css.includes('background:#111'));t('common shell reuse',page.includes('ToolboxSubpageShell'));t('guide common classes',page.includes('toolbox-tool-guide'));t('expert common classes',page.includes('toolbox-tool-expert-post'));t('faq common component',page.includes('ToolboxFaqList'));t('category breadcrumb',page.includes('category/real-estate-build'));console.log(`DESIGN PASS ${p} FAIL ${f}`);process.exitCode=f?1:0;
