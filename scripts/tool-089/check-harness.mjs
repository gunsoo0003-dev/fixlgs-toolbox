import fs from 'node:fs';let p=0,f=0;const t=(n,c)=>{console.log(c?'PASS':'FAIL',n);c?p++:f++};const c=fs.readFileSync('components/tool-089-roof-area-calculator.tsx','utf8');const fixture=JSON.parse(fs.readFileSync('tests/fixtures/tool-089/cases.json','utf8'));for(const s of ['tool089-root','tool089-width','tool089-length','tool089-actual','tool089-material','tool089-error'])t(`selector ${s}`,c.includes(s));t('selector mode template',c.includes('tool089-mode-${m}'));t('fixture canonical30',fixture.canonical30.actual>57&&fixture.canonical30.actual<58);t('fixture 4:12',Math.abs(fixture.riseRun.angle-18.4349488229)<1e-9);t('fixture 90 boundary',fixture.invalidNear90.slopeA===89.999);console.log(`HARNESS PASS ${p} FAIL ${f}`);process.exitCode=f?1:0;
