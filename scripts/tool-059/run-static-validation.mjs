import {spawnSync} from 'node:child_process';const files=['scripts/tool-059/check-source.mjs','scripts/tool-059/check-logic.mjs','scripts/tool-059/check-design.mjs','scripts/tool-059/check-harness.mjs'];let fail=0;for(const f of files){console.log(`\n=== ${f} ===`);const r=spawnSync(process.execPath,['--experimental-strip-types',f],{stdio:'inherit'});if(r.status!==0)fail++;}console.log(`\nTOOL059 STATIC FINAL ${fail?'FAIL':'PASS'} FAIL=${fail}`);if(fail)process.exit(1);
