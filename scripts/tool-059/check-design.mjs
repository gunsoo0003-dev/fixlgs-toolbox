import fs from 'node:fs';let pass=0,fail=0;const c=(n,v)=>{console.log(`${v?'PASS':'FAIL'} ${n}`);v?pass++:fail++;};const css=fs.readFileSync('components/tool-059-pixel-print-converter.module.css','utf8');const ui=fs.readFileSync('components/tool-059-pixel-print-converter.tsx','utf8');const page=fs.readFileSync('components/tool-059-pixel-print-converter-page.tsx','utf8');
for(const x of ['.root','.tabs','.workspace','.resultCard','@media(max-width:820px)','@media(max-width:520px)'])c(`module ${x}`,css.includes(x));for(const x of ['ToolboxSubpageShell','toolbox-tool-detail-hero','toolbox-tool-detail-body','toolbox-next-work','toolbox-tool-guide toolbox-tool-guide--five','toolbox-tool-format-guide toolbox-tool-expert-post toolbox-tool-expert-post--wide-head','toolbox-tool-info-band toolbox-tool-info-band--section-start','toolbox-tool-faq'])c(`shared contract ${x}`,page.includes(x));const order=['toolbox-tool-guide toolbox-tool-guide--five','toolbox-tool-format-guide toolbox-tool-expert-post','toolbox-tool-info-band toolbox-tool-info-band--section-start','toolbox-tool-faq'].map(x=>page.indexOf(x));c('lower DOM exact order HOWTO -> EXPERT -> NOTES -> FAQ',order.every((v,i)=>v>=0&&(i===0||v>order[i-1])));c('expert exact common classes',page.includes('toolbox-tool-format-guide toolbox-tool-expert-post toolbox-tool-expert-post--wide-head'));c('expert cards source six', (page.match(/\['(?:PIXELS \/ PPI|PRINT → PIXELS|EFFECTIVE PPI|PPI ≠ DPI|ASPECT RATIO|QUICK REF)'/g)||[]).length>=6);for(const marker of ['PIXELS / PPI','PRINT → PIXELS','EFFECTIVE PPI','PPI ≠ DPI','ASPECT RATIO','QUICK REF'])c(`expert content marker ${marker}`,page.includes(marker));for(const h of ['픽셀·PPI·실물 크기의 관계를 공식으로 확인하세요','Use the pixel, PPI, and physical-size relationship','ピクセル・PPI・実寸の関係を式で確認します'])c(`locale expert heading ${h}`,page.includes(h));c('category link exact',page.includes('/category/unit-calc'));c('three mode tabs',ui.includes("'pixels-to-print','print-to-pixels','effective-ppi'"));c('mobile single workspace column',/@media\(max-width:820px\)\{\.workspace\{grid-template-columns:1fr\}/.test(css));console.log(`RESULT DESIGN PASS=${pass} FAIL=${fail}`);if(fail)process.exit(1);
