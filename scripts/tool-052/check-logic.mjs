function parts(ms,zone){const f=new Intl.DateTimeFormat('en-CA',{timeZone:zone,year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',second:'2-digit',hourCycle:'h23'});const o={};for(const p of f.formatToParts(new Date(ms)))if(p.type!=='literal')o[p.type]=Number(p.value);return o}function off(ms,z){const p=parts(ms,z);return Math.round((Date.UTC(p.year,p.month-1,p.day,p.hour,p.minute,p.second)-Math.floor(ms/1000)*1000)/60000)}function resolve(p,z){const naive=Date.UTC(p.year,p.month-1,p.day,p.hour,p.minute),probe=[naive-172800000,naive-86400000,naive,naive+86400000,naive+172800000,Date.UTC(p.year,0,15,12),Date.UTC(p.year,6,15,12)],offsets=[...new Set(probe.map(x=>off(x,z)))];const c=[...new Set(offsets.map(o=>naive-o*60000).filter(ms=>{const q=parts(ms,z);return q.year===p.year&&q.month===p.month&&q.day===p.day&&q.hour===p.hour&&q.minute===p.minute}))];return c}
let fail=0;const eq=(n,a,b)=>{const ok=a===b;console.log(`${ok?'PASS':'FAIL'} ${n}: ${a}`);if(!ok)fail++};const ms=Date.UTC(2026,7,19,3,0);eq('Seoul offset',off(ms,'Asia/Seoul'),540);eq('Kolkata offset',off(ms,'Asia/Kolkata'),330);eq('Eucla offset',off(ms,'Australia/Eucla'),525);eq('NY spring nonexistent',resolve({year:2026,month:3,day:8,hour:2,minute:30},'America/New_York').length,0);eq('NY fall ambiguous',resolve({year:2026,month:11,day:1,hour:1,minute:30},'America/New_York').length,2);console.log(`RESULT LOGIC FAIL=${fail}`);process.exit(fail?1:0);
