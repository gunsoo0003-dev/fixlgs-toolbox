import fs from 'node:fs';const h=fs.readFileSync('lib/tool-052-timezone.ts','utf8'),d=fs.readFileSync('lib/tool-052-timezone-data.ts','utf8'),p=fs.readFileSync('components/tool-052-world-time-tool.tsx','utf8');let fail=0;const c=(n,x)=>{console.log(`${x?'PASS':'FAIL'} ${n}`);if(!x)fail++};c('year range 1900-2100',h.includes('minYear: 1900')&&h.includes('maxYear: 2100'));c('12-city limit',h.includes('maxCities: 12')&&p.includes('maxCities'));c('duplicate prevention',p.includes('selected.includes(id)'));c('minimum 2 cities',p.includes('selected.length<=2'));c('invalid working interval rejected',h.includes('a < b'));c('DST ambiguity choice UI',p.includes('tool052-ambiguous')&&p.includes('ambiguityIndex'));c('nonexistent local time UI',p.includes("resolution.kind==='nonexistent'"));c('date boundary badges',p.includes('nextDay')&&p.includes('prevDay'));c('special zones',d.includes('Asia/Kathmandu')&&d.includes('Australia/Eucla')&&d.includes('Pacific/Chatham'));console.log(`RESULT BOUNDARY FAIL=${fail}`);process.exit(fail?1:0);
