import fs from 'node:fs';let p=0,f=0;const c=(n,v)=>{console.log(`${v?'PASS':'FAIL'} ${n}`);v?p++:f++;};const fixture=JSON.parse(fs.readFileSync('tests/fixtures/tool-065/cases.json','utf8'));c('fixtures operations',fixture.operations.length>=5);c('fixtures simplify',fixture.simplify.length>=2);c('fixtures conversion',fixture.fractionToDecimal.length>=2&&fixture.decimalToFraction.length>=2);c('fixtures mixed/error',fixture.mixed.length&&fixture.errors.length>=3);c('limits independent',fixture.limits.maxDigits===100&&fixture.limits.maxInputLength===120&&fixture.limits.decimalPrecision===12);console.log(`RESULT HARNESS PASS=${p} FAIL=${f}`);if(f)process.exit(1);
