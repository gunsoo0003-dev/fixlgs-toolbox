import fs from 'node:fs';import path from 'node:path';const roots=['app','components','lib','scripts','tests'];const exts=new Set(['.ts','.tsx','.js','.mjs','.json','.css','.md','.txt']);const secret=/(api[_-]?key\s*[:=]\s*['"][^'"]{12,}|auth[_-]?token\s*[:=]\s*['"][^'"]{12,}|secret[_-]?key\s*[:=]\s*['"][^'"]{12,}|bearer\s+[a-z0-9._-]{20,})/i;let scanned=0,findings=0;function walk(d){for(const e of fs.readdirSync(d,{withFileTypes:true})){const p=path.join(d,e.name);if(e.isDirectory())walk(p);else if(exts.has(path.extname(e.name))){scanned++;let t='';try{t=fs.readFileSync(p,'utf8')}catch{}if(secret.test(t)){console.log(`FAIL secret ${p}`);findings++;}}}}for(const r of roots)if(fs.existsSync(r))walk(r);console.log(`RESULT SECRET_SCAN ${findings?'FAIL':'PASS'} scanned=${scanned} findings=${findings}`);if(findings)process.exit(1);
