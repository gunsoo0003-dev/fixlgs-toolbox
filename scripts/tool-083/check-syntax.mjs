import fs from 'node:fs';import {createRequire} from 'node:module';const require=createRequire(import.meta.url);let ts;try{ts=require('typescript')}catch{console.error('FAIL typescript dependency unavailable');process.exit(2)}let fail=0;for(const f of ['lib/tool-083-room-area.ts','components/tool-083-room-area-calculator.tsx','components/tool-083-room-area-calculator-page.tsx','app/[locale]/room-wall-ceiling-area-calculator/page.tsx']){const src=fs.readFileSync(f,'utf8');const out=ts.transpileModule(src,{compilerOptions:{jsx:ts.JsxEmit.ReactJSX,target:ts.ScriptTarget.ES2022,module:ts.ModuleKind.ESNext},reportDiagnostics:true,fileName:f});const errs=(out.diagnostics||[]).filter(d=>d.category===ts.DiagnosticCategory.Error);console.log(`${errs.length?'FAIL':'PASS'} syntax ${f}`);if(errs.length){console.log(errs.map(e=>ts.flattenDiagnosticMessageText(e.messageText,' ')).join('\n'));fail++}}process.exitCode=fail?1:0;
