import fs from 'node:fs';import {createRequire} from 'node:module';const require=createRequire(import.meta.url);const ts=require('typescript');let fail=0;const files=['lib/tool-080-rental-yield.ts','components/tool-080-rental-yield-calculator.tsx','components/tool-080-rental-yield-calculator-page.tsx','app/[locale]/rental-yield-calculator/page.tsx'];for(const f of files){const src=fs.readFileSync(f,'utf8');const out=ts.transpileModule(src,{compilerOptions:{jsx:ts.JsxEmit.ReactJSX,target:ts.ScriptTarget.ES2022,module:ts.ModuleKind.ESNext},reportDiagnostics:true,fileName:f});const errs=(out.diagnostics||[]).filter(d=>d.category===ts.DiagnosticCategory.Error);console.log(`${errs.length?'FAIL':'PASS'} syntax ${f}${errs.length?' '+errs.map(e=>ts.flattenDiagnosticMessageText(e.messageText,' ')).join(' | '):''}`);if(errs.length)fail++}process.exitCode=fail?1:0;
