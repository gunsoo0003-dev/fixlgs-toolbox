import fs from 'node:fs';let pass=0,fail=0;const c=(n,v)=>{console.log(`${v?'PASS':'FAIL'} ${n}`);v?pass++:fail++;};const specs=['preflight','design','core','feature','fuel','power','energy','dimension','boundary','regression','limit'];for(const s of specs){const p=`tests/tool-057-${s}.spec.ts`;c(`spec ${s}`,fs.existsSync(p))}const ui=fs.readFileSync('components/tool-057-speed-fuel-energy-converter.tsx','utf8');for(const id of ['tool057-root','tool057-workspace','tool057-value','tool057-from','tool057-to','tool057-swap','tool057-reset','tool057-copy','tool057-error','tool057-result','tool057-main-result','tool057-summary','tool057-precision'])c(`selector ${id}`,ui.includes(id));c('fixtures',fs.existsSync('tests/fixtures/tool-057/cases.json'));c('logic checker',fs.existsSync('scripts/tool-057/check-logic.mjs'));console.log(`RESULT HARNESS_STRUCTURE PASS=${pass} FAIL=${fail}`);if(fail)process.exit(1);
