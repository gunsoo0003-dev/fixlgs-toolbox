TOOL001 MOBILE ROBUSTNESS PATCH V16 - 2026-08-12

Scope
- Product code changed: components/image-converter-tool.tsx ONLY
- TOOL002~024 product code: unchanged
- V15 58-scenario diagnostic suite: retained unchanged

Hardening applied
1) File.arrayBuffer() + FileReader concurrent fallback with 12s guard
2) Filename extension / MIME no longer used as authoritative image-kind validation
3) JPEG validation accepts SOI and delegates final validity to browser decode; terminal EOI/trailing bytes are not hard-rejected
4) URL.createObjectURL() fallback to FileReader data URL for input preview/decode and result preview
5) crypto.randomUUID() fallback to getRandomValues, then timestamp/random fallback
6) blob URL revoke is guarded and only used for blob: URLs

Required next step
- Run: npm run test:toolbox:001-mobile-deep
- Confirm BASE 16/16 + ANDROID 12/12 + CAUSE 30/30 + COVERAGE_MISSING=0
- Do not perform real-device test until automated diagnostic passes after this product patch.
